{% extends 'admin/baseadmin.html.twig' %}

{% block content %}

    <div class="row mt-5 pt-5">
        <div class="col-8 mx-auto">
            <h1>
                Partenaires
            </h1>
        </div>
    </div>

    <div class="row justify-content-center">
        <div class="col-lg-4 col-md-3 col-sm-10 order-md-5 order-lg-5 text-justify m-3 p-3">
            <div class="row alert-warning border border-warning rounded">
                <div class="col-12">
                    <center>
                        <h1>
                            <i class="fas fa-info-circle"></i>
                        </h1>
                    </center>
                    <hr>
                    <p>Vous pouvez ajouter un nouveau partenaire grâce au formulaire sur la gauche. Tous les champs sont obligatoires.
                    </p>
                    <hr>
                    <p>Ces partenaires sont visibles et gérables ci-dessous.</p>
                    <br>
                    <p>Attention ! La suppression d'un partenaire est une
                        <span class="text-danger font-weight-bold">
                            <i class="fas fa-exclamation-triangle"></i>
                            action irréversible
                            <i class="fas fa-exclamation-triangle"></i>
                        </span>!</p>
                </div>
            </div>
            <div class="row m-3">
                <div class="col-12"></div>
            </div>
        </div>

        <div class="col-lg-6 col-md-8 col-sm-10 order-md-0 order-lg-0 text-center m-3 p-3">
            <div class="row">
                <div class="col-12 p-2 alert-info border border-info rounded">
                    <p>
                        Dans cette section, il est possible de modifier les informations relatives aux partenaires de l'entreprise afin de pouvoir les mettre à jour de façon aisée sur le site web.
                    </p>
                </div>
            </div>
            <div class="row mt-3 alert-info border border-info rounded">
                <div class="col-12">
                    <h3>Ajout d'un partenaire</h3>
                    <hr>
                    {{ form_start(PartenairesForm) }}
                    <div class="row">
                        <div class="col-6">
                            {{ form_row(PartenairesForm.nomPartenaire, {'label' : 'Nom du partenaire *'}) }}
                        </div>
                        <div class="col-6">
                            {{ form_row(PartenairesForm.sitePartenaire, {'label' : 'Adresse url du site du partenaire *'}) }}
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-10 mx-auto">
                            {{ form_row(PartenairesForm.file, {'label' : 'Logo du partenaire *',}) }}
                        </div>
                    </div>

                    {{ form_row(PartenairesForm.submit, { 'label' : 'Envoyer', 'attr': {'class': 'col-12 btn btn-outline-success'}})
                                                                                                        }}

                    {{ form_end(PartenairesForm) }}
                </div>
            </div>
        </div>
    </div>


    <div class="row">
        <div class="col-md-10 mx-auto text-center">
            <div class="row text-center">
                {% for logo in logos %}
                    <div class="card col-md-2 m-3 alert-info border border-info rounded p-2">
                        <img class=" card-img-top" src="{{ asset('/photo/logos/' ~ logo.logo) }}" alt="logo de {{logo.nomPartenaire}}">
                        <div class=" card-block text-dark pt-2">
                            <a href="{{logo.sitePartenaire}}">{{logo.nomPartenaire}}</a>
                        </div>
                        <hr>
                        <a class="btn btn-outline-danger text-decoration-none pt-2 pb-0" href="{{ path('delete_partenaire', {id : logo.id } ) }}" onclick="return confirm('Etes-vous certain(e) de vouloir supprimer le partenaire ?')">
                            <h4>
                                <i class="fas fa-trash-alt"></i>
                            </h4>
                        </a>

                    </div>
                {% endfor %}

            </div>
        </div>
    </div>


{% endblock %}
